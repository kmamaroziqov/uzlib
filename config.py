# ============================================================
#  UzSL project config — the ONLY file you need to change
#  on a new machine.
# ============================================================

from pathlib import Path

# Path to the folder containing the `poses/` sub-directory and
# (optionally) a `metadata/` sub-directory with CSVs.
# Accepts absolute paths, e.g.: Path("E:/uzsl_data")
DATA_DIR: Path = Path("D:/uzsl_data_local")
